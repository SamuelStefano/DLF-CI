// Este comentário deve ser sinalizado pelo bot
import { readFileSync } from 'fs';

export const CI_NAME = 'DLF-CI';

// TODO: melhorar isso depois
const resultado = 'teste';

console.log('debug:', resultado);
